export function SiteFooter() {
  return (
    <footer className="mx-auto flex max-w-5xl flex-col gap-2 px-5 py-10 sm:flex-row sm:items-center sm:justify-between">
      <span className="font-mono text-xs text-muted-foreground">
        TAB — reduce · compress · recover · close
      </span>
      <span className="font-mono text-xs text-muted-foreground">Y Combinator · S26</span>
    </footer>
  )
}
